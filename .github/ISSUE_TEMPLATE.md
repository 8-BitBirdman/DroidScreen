## Pre-requirements
- macOS system
- `scrcpy` and `adb` must be accessible globally (via `brew install scrcpy`)

## Required information
1. macOS version
2. Device / phone model
3. scrcpy version (`scrcpy --version`)
4. Can you open the device from the command line using `scrcpy -s <serial>`?
5. Full error message / stack trace
